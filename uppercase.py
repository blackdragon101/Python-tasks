string = input("Enter the string ")
upper_case = lambda word:word.upper()
new_string = upper_case(string)
def invert(up):
    """This is a UDF that takes a lambda function as input and gives out inverted uppercase outputs"""
    print(up[::-1])
    # inverts a string
    return
print(invert(new_string))










